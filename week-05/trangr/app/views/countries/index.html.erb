<h2>Where should we go?</h2>
<%= form_tag "/countries", method: "get", class: "ui form" do %>
  <select name="country_id" class="ui search dropdown">
    <% @countries.each do |c| %>
      <option value="<%= c.id %>"><%= c.name %></option>
    <%end%>
  </select>
  <%= submit_tag "Go", class: "ui button primary" %>
<%end%>
